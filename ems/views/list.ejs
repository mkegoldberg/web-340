<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Employee #</th>
      <th scope="col">First</th>
      <th scope="col">Last</th>
      <th scope="col">Department</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% if (employees.length) { %>
      <% employees.forEach(function (emp) { %>
        <tr>
          <th scope="row"><%= emp.empID %></th>
          <td><%= emp.firstName %></td>
          <td><%= emp.lastName %></td>
          <td><%= emp.department %></td>
          <td><a href="/view/<%= emp.empID %>">View</a></td>
        </tr>
      <% }) %>
    <% } else { %>
      <tr>
        <td>
          Sorry you don't have any employees yet!<br />
          Click <a href="/new" class="">HERE</a> to add one
        </td>
      </tr>
    <% } %>
  </tbody>
</table>
