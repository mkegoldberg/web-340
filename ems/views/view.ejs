<!DOCTYPE html>
<html lang="en">
<%- include("header"); %>

<body>
  <%- include("navbar"); %>

  <div role="main" class="container" style="width: 50%;">
    <br /><br />

    <h2>Employee Record</h2>
    <br />

    <div class="card">
      <h5 class="card-header">
        <svg class="bi bi-people-circle mr-2 -mt-2" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor"
          xmlns="http://www.w3.org/2000/svg">
          <path
            d="M13.468 12.37C12.758 11.226 11.195 10 8 10s-4.757 1.225-5.468 2.37A6.987 6.987 0 008 15a6.987 6.987 0 005.468-2.63z" />
          <path fill-rule="evenodd" d="M8 9a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd" />
          <path fill-rule="evenodd" d="M8 1a7 7 0 100 14A7 7 0 008 1zM0 8a8 8 0 1116 0A8 8 0 010 8z" clip-rule="evenodd" />
        </svg>
        Employee Details
      </h5>

      <div class="card-body">
        <div class="title mb-2">
          <% if (employees[0].lastName) { %>
            <input type="text" class="read-only" id="fullName" readonly value="<%= employees[0].firstName %> <%= employees[0].lastName %>" name="fullName" />
          <% } else { %>
            <input type="text" class="read-only" id="firstName" readonly
              value="<%= employees[0].firstName %>" name="firstName" />
          <% } %>
        </div>

        <div class="details">
          <% if (employees[0].empID) { %>
            <div>
              <label class="w-8 font-weight-light">ID:</label>
              <input type="text" class="read-only" id="empID" readonly value="<%= employees[0].empID %>" name="empID" />
            </div>
          <% } %>
          <% if (employees[0].department) { %>
            <div class="-mt-2">
              <label class="w-8 font-weight-light">Department:</label>
              <input type="text" class="read-only" id="department" readonly value="<%= employees[0].department %>" name="department" />
            </div>
          <% } %>
        </div>
      </div>
    </div>

    <br /><br />

    <a href="/">&larr; OK</a>
  </div>

  <%- include("footer"); %>
  <%- include("scripts"); %>
</body>

</html>
